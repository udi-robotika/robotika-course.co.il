ROBOTIKA-COURSE.co.il
Current version: V82 (sensors.html only)
Based on: V81
Fix: light sensor card description shortened to fit 2 lines max — removed pin details ("4 פינים VCC, GND, DO, AO"), kept just "מודול LDR עם פלט אנלוגי ודיגיטלי"
Added 8 more components to the "רכיבים נוספים — לא בשימוש בקורס" section: flame/fire sensor, PIR motion sensor, water level sensor, MPU-6050 gyro+accelerometer, gas sensor (MQ family), soil moisture sensor, VL53L0X ToF laser distance sensor, and a collection of extra microphone/sound modules. All images saved under images/sensors/extra/
Added: new "רכיבים נוספים — לא בשימוש בקורס" section at the bottom of sensors.html, visually separated (dashed border, muted/non-clickable cards) from the main course sensor grid. First item: a high-power industrial laser diode module (images/sensors/extra/laser-module.jpg), per Udi's photo. More items can be added to this same section later
Fix: laser sensor card image replaced with Udi's photo of the actual KY-008 style laser diode module (3-pin, S/+/- labeled). Image saved to images/sensors/laser.jpg (overwritten)
Fix: images/sensors/ir.png had huge amounts of white padding around the actual sensor (content only filled ~40% of the canvas height), making it look tiny in the 80px-tall card thumbnail. Cropped tightly to content (416x416 → 292x189) so it now fills the card properly
Added 3 new sensors, per Udi's comparison against another reference site: sensor-temp-humidity.html (DHT11 — temperature+humidity), sensor-sound.html (sound/clap sensor module), sensor-rtc.html (DS3231 real-time clock). Each is a full detail page (specs, wiring, example code, exercise idea) matching the site's existing template
New cards added to sensors.html grid for all 3, with emoji icon placeholders (no product photos yet — Udi to send real photos to replace the placeholders, same workflow as the light/potentiometer/joystick image swaps earlier)
Nav dropdown under "חיישנים" updated site-wide (34 shared pages) to include all 3 new sensor links
Footer version bumped to V77 across all touched/shared pages
Fix: images/sensors/joystick.jpg was actually corrupted — only 114 bytes of stray HTML redirect text, not a real image (pre-existing bug, unrelated to any of this session's changes, likely present since before V42). Replaced with Udi's real photo of the KY-023 module. Scanned all other site images for the same corruption pattern — none found, this was the only one
Fix: potentiometer card image replaced — was a Grove-style breakout module photo, now shows the actual bare 3-pin rotary potentiometer used in the course. Image saved to images/sensors/potentiometer.jpg (overwritten)
Fix: light sensor card image replaced — was a plain 3-pin LDR photo, now shows the actual 4-pin module (VCC, GND, DO, AO) with both analog and digital output, matching what's really used in the course. Card description updated accordingly. Image saved to images/sensors/light.jpg (overwritten)
Fix: sensors.html card grid changed from auto-fit (~3 columns) to a fixed 5-column layout on desktop, with smaller images (120px → 80px) and tighter card padding. Added responsive fallbacks: 3 columns between 781-1000px, 2 columns under 780px (images 70px)
Restructure: the two joke images now sit side by side in a 2-column gallery (stacks to 1 column under 600px). Each image is a clickable link with a "מה מאחורי הבדיחה →" hint, jumping via same-page anchor to its own detail section below (#detail-1 = Nietzsche quote source, #detail-2 = new explanation of the "חי" gematria/חיישנים pun for the birthday joke)
Fix: moved the subtitle line ("קצת צחוקים בין הלחמות...") from under the top badge to the bottom of the page (after the joke cards, before the footer), per Udi's request to reduce wasted space at the top
Added: second joke image (images/jokes/arduino-joke-02.png, 1254x1254, full resolution) — Arduino's 18th birthday pun ("מה קונים לארדואינו ליום הולדת 18? חי״ו-שנים!"), placed right after the Nietzsche quote source block
Fix: found that /lessons/course-43/index.html and /lessons/course-43/lesson-1/index.html never received any of the mobile nav fixes from V63-V67, because those pages live in a subfolder and were built/edited separately from the main *.html glob used for the site-wide sed fixes. Applied the same final fix (flex-wrap nav, centered stacked header, direction:rtl, display:none dropdowns) to both files directly
Cleanup: removed the two diagnostic test pages (mobile-nav-test.html, minimal-test.html) now that Udi confirmed the mobile nav fix works correctly on the real pages
Real root cause found and fixed (site-wide, mobile only): the dropdown submenus (.nav-dropdown, e.g. under "חיישנים", "בסיס") stayed in the DOM with opacity:0/visibility:hidden when closed, each min-width:220px and position:absolute. On mobile, once the top nav wraps into multiple rows, these still-invisible-but-space-occupying dropdown boxes silently inflated the page's total scrollable width beyond the viewport (confirmed by Udi's minimal-test.html working perfectly — proving it wasn't a device/browser/proxy issue, only present on pages with this dropdown structure). Fixed by switching closed dropdowns to display:none on mobile (fully removed from layout/overflow calculations) and only display:flex (stacked, static position) when a menu is hovered/focused. Applied to all 34 shared-nav pages and components-gallery.css
Diagnostic pages added this round: mobile-nav-test.html, minimal-test.html (both noindex, unlisted — safe to delete once Udi confirms the real pages are fixed)
---
ROBOTIKA-COURSE.co.il
Current version: V66 (unchanged — this adds a new diagnostic page only)
New: mobile-nav-test.html — a brand-new, never-before-seen URL (copy of sensors.html's header/nav) created specifically to rule out any caching (browser, incognito, or Chrome Data Saver proxy) as the cause of the mobile nav layout complaint Udi keeps seeing. noindex, not linked from nav. Delete once the mobile nav question is resolved either way
Fix (site-wide, mobile only, root cause): found the actual cause of the mobile nav clipping Udi kept seeing after V63/V64 — the header element has direction:ltr (needed for the desktop logo-left/nav-right layout), which on a dir="rtl" page mismatches when the header switches to a stacked column on mobile, pushing wrapped nav rows past the right edge of the viewport instead of centering them. Fixed by forcing direction:rtl on the header specifically inside the mobile media query. Also added body{overflow-x:hidden} on mobile as a defensive safety net against any further stray horizontal overflow. Applied to all 34 shared-nav pages and components-gallery.css
Added below the joke image: (1) a playful line about a "rare collaboration" between Nietzsche and Udi Wallach producing the joke, (2) a sourced note with Nietzsche's real original quote in Hebrew and German ("Was mich nicht umbringt, macht mich stärker")
Fix (site-wide, mobile only, redesign): Udi reported the mobile nav still looked bad/uncentered after V63's flex-wrap fix. Redesigned mobile header: header now stacks as a centered column (logo centered above nav), and every nav item (plain links, dropdown labels, CTA pills) gets a consistent rounded "chip" style (light background, border, generous padding) so the wrapped row reads as an intentional centered button cluster instead of mismatched plain text links. Applied across all 34 shared-nav pages and components-gallery.css. Dropdown submenu behavior itself left untouched (pre-existing hover-based mechanism, out of scope for this fix)
Fix (site-wide, mobile only): the mobile navigation menu (@media max-width:780px) used a fixed 3-column CSS grid, which clipped/truncated longer nav items (e.g. "רכיבים לקנייה", "קוד משותף", "ESP32") on real phone screens — confirmed by Udi's screenshot. Changed to a flex-wrap layout so every nav item sizes to its own text and wraps naturally onto as many rows as needed, with no clipping. Applied identically across all 34 shared-nav pages and to components-gallery.css (used by the 5 components pages). live-code.html untouched (it has its own separate minimal header, no shared nav)
Footer: version bumped to V63 across all affected pages
Fix: merged the separate large H1 ("הומור וארדואינו") into the badge/pill above it — badge now shows the title text directly (larger, bold), the standalone H1 line removed, cleaned up now-unused CSS. One less line of vertical space at the top of the page
---
ROBOTIKA-COURSE.co.il
Current version: V61 (arduino-jokes.html only — other pages remain V55)
Based on: V60
Fix: compacted the header area of arduino-jokes.html — reduced top margin, badge/title/subtitle spacing and sizes, so the whole block takes noticeably less vertical height before the joke image
---
ROBOTIKA-COURSE.co.il
Current version: V60 (arduino-jokes.html only — other pages remain V55)
Based on: V59
Fix: subtitle text under "הומור וארדואינו" shortened ("כי גם" → "—") and given max-width:32ch (centered) so it reliably wraps to 2 lines instead of 3, keeping the "בין הלחמות" pun intact
---
ROBOTIKA-COURSE.co.il
Current version: V59 (arduino-jokes.html only — other pages remain V55)
Based on: V58
Fix: replaced images/jokes/arduino-joke-01.png with Udi's newer preferred render (same 1254x1254 resolution, with credit) — display still capped to 50% width via CSS
---
ROBOTIKA-COURSE.co.il
Current version: V58 (arduino-jokes.html only — other pages remain V55)
Based on: V57
Fix (correction of misunderstanding): restored images/jokes/arduino-joke-01.png to full original resolution (1254x1254, ~2.3MB) — Udi wanted high resolution kept, just displayed smaller on the page. Changed approach: image file stays full-res, CSS now caps display width to 50% of the content column (max-width:50%, centered), with a mobile override back to 100% width under 700px so it doesn't become tiny on phones
---
ROBOTIKA-COURSE.co.il
Current version: V57 (arduino-jokes.html only — other pages remain V55)
Based on: V56
Fix: images/jokes/arduino-joke-01.png shrunk by 50% (919x919 → 459x459), re-rendered from original source for sharpness
---
ROBOTIKA-COURSE.co.il
Current version: V56
Based on: V55
Removed: old /jokes.html redirect deleted entirely per Udi's request — only /arduino-jokes.html works now (same lesson learned as the earlier /course-july-2026/ deletion: Udi prefers old paths gone outright, not left as redirects)
---
ROBOTIKA-COURSE.co.il
Current version: V55
Based on: V51 (main site pages) / V54 (jokes page, renamed this round)
Restructure: jokes.html renamed to arduino-jokes.html per Udi's request; old /jokes.html now redirects there. Nav link updated across all 34 shared pages.
---
ROBOTIKA-COURSE.co.il
Current version: V54 (jokes.html only — other pages remain V51)
Based on: V53
Fix: images/jokes/arduino-joke-01.png enlarged by 50% per Udi's request (613x613 → 919x919). Re-rendered directly from the original full-resolution upload (not upscaled from the shrunk 613px version) to keep quality sharp
---
ROBOTIKA-COURSE.co.il
Current version: V53 (jokes.html only — other pages remain V51)
Based on: V52
Fix: images/jokes/arduino-joke-01.png shrunk by another 30% (877x877 → 613x613, ~960KB → ~532KB), per Udi's follow-up request
---
ROBOTIKA-COURSE.co.il
Current version: V52 (jokes.html only — other pages remain V51)
Based on: V51
Fix: images/jokes/arduino-joke-01.png resized to 70% of original dimensions (1254x1254 → 877x877, ~2.3MB → ~960KB), per Udi's request to shrink the joke image by 30%
---
ROBOTIKA-COURSE.co.il
Current version: V51
Based on: V49 (main site pages) / V50 (lesson-6, left untouched this round)
Footer: version bumped to V51 across all 34 shared-nav pages + lessons/course-43 + makelab/july-2026 hub
New page: jokes.html ("הומור") — Arduino/electronics humor, includes Udi's own AI-generated joke image (images/jokes/arduino-joke-01.png, credited "© UDI WALLACH | robotika.co.il") plus a few short original puns
Main nav: added "הומור" link (between "שיעורים" and "ESP32") across all 34 shared pages
---
ROBOTIKA-COURSE.co.il
Current version: V50
Based on: V49
Note: this version bump applies only to /makelab/july-2026/lesson-6/index.html (per Udi's explicit request not to touch other pages this time) — footer version tag on all other pages remains V49
Added: new section "מה זה בכלל ESP32?" to lesson 6 — history (Espressif, released Sept 2016, successor to ESP8266), why choose it over Arduino Uno (Wi-Fi/Bluetooth built in, 240MHz dual-core vs 16MHz single-core, ~520KB vs ~2KB RAM), full comparison table, and a 3.3V vs 5V safety warning
No web images embedded (hotlinked images into a permanent public page risk breaking or copyright issues) — used an original comparison table/chips instead; flagged this decision to Udi
---
ROBOTIKA-COURSE.co.il
Current version: V49
Based on: V48
Footer: version bumped to V49 across all pages
Removed: /course-july-2026/ redirect page deleted entirely per Udi's request (old URL no longer resolves at all — only /makelab/july-2026/ works now)
---
ROBOTIKA-COURSE.co.il
Current version: V48
Based on: V47
Footer: version bumped to V48 across all pages
Restructure: /course-july-2026/ moved to /makelab/july-2026/ (Udi's requested URL); old /course-july-2026/ now redirects there, still noindex/unlisted
---
ROBOTIKA-COURSE.co.il
Current version: V47
Based on: V46
Footer: version bumped to V47 across all pages
New: /course-july-2026/lesson-6/ — MAKELAB יהוד, שיעור 6 (ESP32 setup, servo motor, smart-bin project) + homework, noindex, unlisted
Updated: /course-july-2026/ hub now links to lesson 6 (was an empty "coming soon" placeholder)
---
ROBOTIKA-COURSE.co.il
Current version: V46
Based on: V45
Footer: version bumped to V46 across all pages
Branding: /course-july-2026/ updated to "MAKELAB יהוד" (title, badge, h1, footer) — was generic "קורס יולי 2026"
---
ROBOTIKA-COURSE.co.il
Current version: V45
Based on: V44
Footer: version bumped to V45 across all pages
Fix: /lessons/course-43/ heading changed from "סולמית 43" to "קורס #43 · ימי שלישי" (per Udi's correction — title, browser tab title, and footer copyright line all updated)
---
ROBOTIKA-COURSE.co.il
Current version: V44
Based on: V43
Footer: version bumped to V44 across all pages
Restructure: /lessons/lesson-1/ moved to /lessons/course-43/lesson-1/ ("סולמית 43" — Dizengoff Center location, course #43)
New: /lessons/course-43/ is now the course-43 lessons hub (moved from /lessons/); /lessons/index.html is now a redirect to /lessons/course-43/ for backward compatibility with the site nav
New isolated section: /course-july-2026/ — separate top-level hub for the second location (Yehud), not linked from any nav and marked noindex, so its students and the Dizengoff course-43 students never see each other's lessons
No existing page content, images, or the live-code Blob API/storage was modified
---
ROBOTIKA-COURSE.co.il
Current version: V43
Based on: V42
Footer: version bumped to V43 across all 36 pages
New section: /lessons/ — lesson summaries hub, linked from main nav ("שיעורים")
New page: /lessons/lesson-1/ — שיעור 1 summary (Arduino pins, PWM, LED blink, Sensor Shield, light sensor) + homework tasks
Main nav: added "שיעורים" link (between "קוד משותף" and "ESP32") across all 34 existing pages, pointing to /lessons/
No existing page content, images, or the live-code Blob API/storage was modified
Footer: version bumped to V42
Deleted: components-course.html ("מומלץ לקורס" / "רכיבים מומלצים לקורס") removed entirely from the site — its 11 items are gone
package-nav pill bar updated across all 5 remaining component pages to remove the dead link
Hub page (recommended-components.html): category-grid card for "רכיבים מומלצים לקורס" removed — 4 cards remain
New page: recommended-components.html
Updated: 2026-08-29
Cleanup: removed duplicate index (1).html
Layout: navigation on the right and site title on the left across all pages
Components: added four-page visual gallery with six cards per desktop row
Images: added real product photos for the required starter components
Local update: corrected three product images and added AliExpress purchase links
Categories: renamed the starter category to "בסיס מומלץ"
Categories: added "אודי ממליץ בחום" with three selected products
Live code: added a simple anonymous classroom board with title, paste, publish, copy and .ino download
Live code: persistent Vercel Blob storage, available from every computer and across future lessons
Deployment: deploy-to-vercel.cmd now creates and connects Blob storage automatically on first run
Live code fix: storage listing now reads every saved JSON file, follows pagination and retries failed reads
Live code fix: refreshing no longer removes codes already loaded on the page
Udi recommends page: added ESP32-S3 WROOM-1 N16R8 card as the first item
Udi recommends page: reordered cards to ESP32-S3, TFT screen, GPIO expansion, Arduino Uno shield
Hub page: updated item count for "אודי ממליץ בחום" from 3 to 4
Udi recommends page: swapped in the real annotated ESP32-S3 WROOM-1 hardware photo
Udi recommends page: swapped in the real TFT 2.4" product photo
Homepage: added "מתחם לימודים" link row above the footer, linking to https://sites.google.com/view/robotika-course
Component pages: reordered package-nav pills to בסיס מומלץ, אודי ממליץ בחום first (highlighted), then the rest, across all 5 component pages
Main nav: renamed "לוח קודים" to "קוד משותף" across all 34 pages
Main nav: removed the small dropdown arrow (⌄) after חיישנים/בסיס/פרויקטים/תקשורת, dropdown-on-hover behavior unchanged
Main nav: styled "רכיבים לקנייה" and "קוד משותף" as a highlighted gradient pill button, across all 34 pages
Main nav: restyled those two pills from solid gradient to an outlined purple pill, matching the package-nav featured look
